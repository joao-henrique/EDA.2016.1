#include <stdio.h>

int main() {
	int T;
	float X;
	
	scanf("%f %d", &X, &T);
	
	if(T <= 100)
		printf("VALOR DA CONTA = R$ %.2f\n", X);
	else {
		T -= 100;
		X += T*0.15;
		printf("VALOR DA CONTA = R$ %.2f\n", X);
	}

return 0;
}
