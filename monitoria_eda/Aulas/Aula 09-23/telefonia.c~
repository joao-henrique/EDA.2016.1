#include <stdio.h>
#include <stdlib.h>

int main() {
	int T;
	double X;
	
	scanf("%d %lf", &X, &T);
	
	if(T <= 100)
		printf("VALOR DA CONTA = R$ %.2lf\n", X);
	else {
		T -= 100;
		X += T*0.15;
		printf("VALOR DA CONTA = R$ %.2lf\n", X);
	}

return 0;
}
